import React from 'react';

const Logout = () => {
  const handleLogout = () => {
    // Clear user authentication state or invalidate the session
  };

  return (
    <button onClick={handleLogout}>Logout</button>
  );
};

export default Logout;
